"""CMS Compliance Framework.

Project-agnostic, filename-driven framework: batch registration, file intake, validation,
promotion to core, manual overrides (reuse, late arrival, correction), period validation and
closing the run whose data the project's extract job then reads.
See docs/design/cms-compliance-framework-design.md and docs/module-reference.md.
"""

__version__ = "0.3.0"
